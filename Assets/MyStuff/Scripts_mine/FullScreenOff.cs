using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FullScreenOff : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
       // FullScreen.Instance.gameObject.SetActive(false);
    }


}
