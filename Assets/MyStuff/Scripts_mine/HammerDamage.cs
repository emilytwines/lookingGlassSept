using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HammerDamage : MonoBehaviour
{
    /*

        private bool _isCausingDamage;
       /// public float DamageRepeatRate = 0.1f;
        public int DamageAmount = 20;
        public bool Repeating = false;


    private void Start()
    {
      Collider sink = GameObject.Find("Sink").GetComponent<Collider>();

    }



    private void OnTriggerEnter(Collider sink)
        {  
            _isCausingDamage = true;

            SinkEnemyController player = GetComponent<SinkEnemyController>();

            if (player != null)
            {
            player.TakeDamage(DamageAmount);
              
            }
        }



        private void OnTriggerExit(Collider sink)
        {
          SinkEnemyController player = GetComponent<SinkEnemyController>();

          if (player != null)
             { 
                _isCausingDamage = false;
              }

        }


    */
}
